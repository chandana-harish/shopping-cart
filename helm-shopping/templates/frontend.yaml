apiVersion: apps/v1 
kind: Deployment 
metadata:
  name: frontend-deployment 
  namespace: sp 
spec:
  replicas: 1
  strategy:
    type: RollingUpdate 
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector: 
    matchLabels:
      app: shop  
  template:
    metadata:
      labels:
        app: shop 
    spec:
      containers:
        - name: shopping-cont 
          image: suryaa112003/shopping:v1.1.0
          port: 
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "250Mi" 
            limits:
              cpu: "200m" 
              memory: "400Mi" 
          
  
  
  
